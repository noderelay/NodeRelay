name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
          - os: windows-2025
          - os: macos-latest

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install system deps (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake ninja-build \
            libgl-dev libsecret-1-dev \
            libfuse2 librsvg2-bin

      - name: Install Qt (Linux)
        if: runner.os == 'Linux'
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.8.2'
          modules: 'qtwebsockets'
          cache: true

      - name: Setup Python (Windows)
        if: runner.os == 'Windows'
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Qt (Windows)
        if: runner.os == 'Windows'
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.8.2'
          arch: 'win64_msvc2022_64'
          modules: 'qtwebsockets'
          cache: true

      - name: Install Qt (macOS)
        if: runner.os == 'macOS'
        run: brew install qt cmake ninja qtkeychain

      - name: Configure (Linux)
        if: runner.os == 'Linux'
        run: cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DUPLINK_VENDOR_DEPS=ON

      - name: Configure (Windows)
        if: runner.os == 'Windows'
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Configure (macOS)
        if: runner.os == 'macOS'
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH=$(brew --prefix qt)

      - name: Build
        run: cmake --build build --config Release

      # ── Linux ────────────────────────────────────────────────────────────
      - name: Package (Linux)
        if: runner.os == 'Linux'
        run: |
          mkdir -p artifact/Uplink
          cp build/Uplink artifact/Uplink/
          cp -r build/themes artifact/Uplink/
          tar -czf Uplink-${{ github.ref_name }}-linux-x86_64.tar.gz \
              -C artifact Uplink

      - name: Build AppImage (Linux)
        if: runner.os == 'Linux'
        env:
          UPDATE_INFORMATION: "gh-releases-zsync|${{ github.repository_owner }}|${{ github.event.repository.name }}|latest|Uplink-*-x86_64.AppImage.zsync"
          UPLINK_EXTRA_CMAKE_ARGS: "-DUPLINK_VENDOR_DEPS=ON"
          CMAKE_PREFIX_PATH: ${{ env.QT_ROOT_DIR }}
        run: bash packaging/build-appimage.sh

      # ── Windows ──────────────────────────────────────────────────────────
      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path artifact\Uplink | Out-Null
          Copy-Item build\Release\Uplink.exe artifact\Uplink\
          Copy-Item -Recurse build\Release\themes artifact\Uplink\themes
          $qkcDll = Get-ChildItem -Path build -Recurse -Filter qt6keychain.dll | Select-Object -First 1
          if ($qkcDll) { Copy-Item $qkcDll.FullName "$env:QT_ROOT_DIR\bin\" }
          windeployqt --release artifact\Uplink\Uplink.exe
          Compress-Archive -Path artifact\Uplink\* `
              -DestinationPath Uplink-${{ github.ref_name }}-windows-x64.zip

      # ── macOS ────────────────────────────────────────────────────────────
      - name: Package (macOS)
        if: runner.os == 'macOS'
        run: |
          mv build/Uplink.app/Contents/MacOS/themes build/Uplink.app/Contents/Resources/themes
          $(brew --prefix qt)/bin/macdeployqt build/Uplink.app
          find build/Uplink.app/Contents/Frameworks -name "*.dylib" -exec codesign --force --sign - {} \;
          find build/Uplink.app/Contents/Frameworks -type d -name "*.framework" -exec codesign --force --sign - {} \;
          find build/Uplink.app/Contents/PlugIns -name "*.dylib" -exec codesign --force --sign - {} \;
          codesign --force --sign - build/Uplink.app/Contents/MacOS/Uplink
          codesign --force --sign - build/Uplink.app
          ARCH=${{ runner.arch == 'ARM64' && 'arm64' || 'x86_64' }}
          DMG_NAME="Uplink-${{ github.ref_name }}-macos-${ARCH}.dmg"
          hdiutil create -volname "Uplink" -srcfolder build/Uplink.app \
              -ov -format UDZO "${DMG_NAME}"

      # ── Upload ───────────────────────────────────────────────────────────
      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            Uplink-${{ github.ref_name }}-*
            Uplink-*.AppImage
            Uplink-*.AppImage.zsync
          generate_release_notes: true

